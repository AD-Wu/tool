package com.x.framework.caching.datas.matchers;

public enum LikeComparer implements IComparer {
    ;

    @Override
    public boolean compare(Object first, Object second) {
        return false;
    }
}
